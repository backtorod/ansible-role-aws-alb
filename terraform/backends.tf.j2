#jinja2: lstrip_blocks: "True", trim_blocks: "False"

terraform {
  backend "s3" {
    bucket = "{{ terraform_state_bucket }}"
    key    = "{{ terraform_state_key }}"
    region = "{{ aws_region }}"
  }
}
