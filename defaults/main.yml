---
# defaults file for ansible-role-aws-alb

templates:
  - alb
  - backends
  - outputs
  - variables

terraform:
  version: 0.12
  args: -no-color
  state:
    bucket: terraform-cloud44-states
    key: develop/tests

terraform_state_key: 88888

providers:
  aws:
    version: 2.33.0

aws:
  region: "{{ survey_aws_region | default('us-east-1') }}"